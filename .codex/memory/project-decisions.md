# Quyết định dự án

- Chạy local-only, không cloud.
- Giới hạn bộ nhớ mục tiêu 5 GB.
- Chỉ giữ `recording.webm` và `minutes.json` sau khi thành công.
- Owner/deadline không rõ phải để `null` và `needs_confirmation`.
