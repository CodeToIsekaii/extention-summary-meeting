# Start backend

- Cách mới: chạy `scripts\start-desktop.bat` để mở desktop app và backend cùng lúc.
- Cách thủ công: chạy `scripts\start-helper.ps1` hoặc `scripts\start-helper.sh`.
- Backend khỏe khi `http://127.0.0.1:8765/v1/health` trả `status: ok`.
